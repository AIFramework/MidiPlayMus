namespace Midi.NoteSeqData.Base
{
    public enum EncodingType
    {
        MIDI
    }

    public enum Parser
    {
        PRETTY_MIDI
    }

    public enum Instrument
    {
        Default
    }
}
